{
  "mcpServers": {
    "codegraph": {
      "command": "/home/ybao/.local/bin/codegraph",
      "args": [
        "serve"
      ],
      "env": {
        "CODEGRAPH_DB": ".codegraph/codegraph.db",
        "RUST_LOG": "off"
      },
      "serverInstructions": "ALWAYS prefer CodeGraph tools over Grep/Glob/Explore for code search, dependency analysis, and symbol lookup. The pre-built AST index provides instant results across 32 languages."
    }
  }
}
