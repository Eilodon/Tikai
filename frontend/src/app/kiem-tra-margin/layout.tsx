import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Kiểm Tra Margin TikTok Shop — Tikai",
  description:
    "Kiểm tra margin thực của sản phẩm TikTok Shop. Tính đủ phí sàn, hoa hồng, voucher. Biết ngay có lãi hay lỗ.",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
