import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Tính Hoa Hồng Creator TikTok Shop — Tikai",
  description:
    "Tính chính xác hoa hồng affiliate creator TikTok Shop. Tính cả kịch bản hoàn hàng — hoa hồng bị mất không thu hồi.",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
